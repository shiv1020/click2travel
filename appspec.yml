version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/html

hooks:
  AfterInstall:
    - location: scripts/permissions.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: scripts/restart.sh
      timeout: 300
      runas: root
